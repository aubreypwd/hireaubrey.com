<section id="cta" class="{{ sections.cta.section.class | default( '' ) }}">
	<div class="content centered {{ sections.cta.content.class | default( 'bordered' ) }}">
		<p class="lead">I work exclusively with <strong>local businesses</strong> I think are the <strong class="emu">best</strong> for our community.</p>
		<h2>Are you one of those <em>great</em> local businesses?</h2>
		{% include 'book-button.njk' %}
	</div>
</section>